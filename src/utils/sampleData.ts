import { Subject, ClassItem, UserPreferences } from '../types';

export const defaultPreferences: UserPreferences = {
  name: '',
  globalTarget: 75,
  theme: 'light',
  semesterBreak: false
};

export const defaultSubjects: Subject[] = [];

export const defaultClasses: ClassItem[] = [];

